# Exemplo histórico (pizzaria)

Demo Electron usado para desenvolver a primeira versão do arnês.
Não faz parte do kit portável — use o `README.md` da raiz do repositório
e `./install.sh` para aplicar o arnês em outros projetos.

```bash
cd examples/pizzaria
./init.sh
npm test
```
